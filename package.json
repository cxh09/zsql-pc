{
  "name": "zsql-pc",
  "version": "2.3.0",
  "description": "ZSQL PC - Electron Application with TDesign",
  "main": "main.js",
  "homepage": "https://github.com/zsql/zsql-pc",
  "scripts": {
    "dev:vite": "vite",
    "dev:electron": "wait-on http://localhost:5173 && cross-env VITE_DEV_SERVER_URL=http://localhost:5173 electron . --dev",
    "dev": "concurrently -k -n vite,electron -c blue,green \"npm:dev:vite\" \"npm:dev:electron\"",
    "start": "npm run build:vite && electron .",
    "check-icons": "node scripts/check-icons.mjs",
    "gen-placeholder-icons": "node scripts/gen-placeholder-icons.mjs",
    "build-icons": "node scripts/build-icons.mjs",
    "build:vite": "vite build",
    "build": "npm run check-icons && npm run build:vite && electron-builder",
    "build:win": "npm run check-icons && npm run build:vite && electron-builder --win",
    "build:mac": "npm run check-icons && npm run build:vite && electron-builder --mac",
    "build:linux": "npm run check-icons && npm run build:vite && electron-builder --linux",
    "build:all": "npm run check-icons && npm run build:vite && electron-builder --win --linux"
  },
  "keywords": [
    "electron",
    "vue",
    "tdesign"
  ],
  "author": "ZSQL Team <chenxinhang2009@gmail.com>",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@vitejs/plugin-vue": "^6.0.7",
    "concurrently": "^10.0.3",
    "cross-env": "^10.1.0",
    "tdesign-vue-next": "^1.20.0",
    "vite": "^8.0.16",
    "vue": "^3.5.35",
    "wait-on": "^9.0.10"
  },
  "devDependencies": {
    "electron": "^42.3.1",
    "electron-builder": "^26.15.3"
  },
  "build": {
    "appId": "com.zsql.pc",
    "productName": "ZSQLPC",
    "directories": {
      "output": "dist"
    },
    "files": [
      "main.js",
      "preload.js",
      "dist/renderer/**/*",
      "pages/**/*",
      "libs/**/*",
      "assets/**/*",
      "package.json"
    ],
    "win": {
      "target": "nsis",
      "icon": "build/icon.ico"
    },
    "mac": {
      "target": "dmg",
      "icon": "build/icon.icns"
    },
    "linux": {
      "target": [
        {
          "target": "tar.gz",
          "arch": [
            "x64"
          ]
        },
        {
          "target": "deb",
          "arch": [
            "x64"
          ]
        }
      ],
      "icon": "build/icon.png",
      "category": "Utility"
    }
  }
}
